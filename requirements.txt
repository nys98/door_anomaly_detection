# Python dependencies for the door condition monitoring notebook.
# Standard-library modules used (no installation needed): json, ast, glob, os.
# Versions are pinned to the environment this notebook was developed and run in.

pandas==2.3.3
numpy==2.4.1
matplotlib==3.10.8
scikit-learn==1.9.0
